<!--

Setup steps

1. npx create-react-app my-app --template typescript
2. cd my-app
3. add components (.ts and .tsx)
4. Typescript should automatically start showing errors (tsconfig strict: true)
5. If not, reload the window
6. If it still doesn't show, enable the typescript plugin for vscode

 -->
